Q.1 Explain the concept of recursion and how it can simplify certain problems?
    ->its a method that call itself recursively to solve a smaller version of the same problem.

Q.2 Discuss the time complexity of your recursive algorithm?
  ->Time complexity is O(n) .since recursive call decrease by 1 year.so if n years are there ,then time complexity will be O(n).
  ->also the space complexity is O(n).since each recursive call is stored on call stack.

Q.3 Explain how to optimize the recursive solution to avoid excessive computation?
  -> here ,we can use the iteration instead of the recursive approach .Since there are no recursive call so space complexity will become O(1).
